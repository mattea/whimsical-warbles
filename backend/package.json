{
  "name": "pugglenaut-api",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker backend for the Pugglenaut personal website.",
  "scripts": {
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "db:init": "wrangler d1 execute pugglenaut --remote --file schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260903.1",
    "wrangler": "^4.128.0"
  }
}
