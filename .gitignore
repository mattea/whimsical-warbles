# build output (root only — the vendored design-system dist/ must stay tracked)
/dist/
.astro/

# dependencies
node_modules/

# logs
*.log
npm-debug.log*

# environment
.env
.env.production

# macOS
.DS_Store
